# EigenAI.CLI

Command-line interface for the EigenAI SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global EigenAI.CLI --prerelease
```

## Usage

```bash
eigen-ai --help
eigen-ai api --help
```